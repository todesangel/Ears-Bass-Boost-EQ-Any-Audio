# 🎧 Ears: Bass Boost, EQ Any Audio! (Firefox / Zen)

`Ears` — расширение (Manifest V3) для **Firefox** и браузеров на его основе (Zen), которое
позволяет эквализировать звук во вкладках в реальном времени: усиливать бас, приглушать высокие,
поднимать вокал и сохранять собственные пресеты.

## 🔁 Порт с Chrome: что изменилось

Оригинальная версия была написана под Chrome и опиралась на два API, которых **нет в Firefox**:

- `chrome.tabCapture.getMediaStreamId()` вместе с проприетарным ограничением
  `getUserMedia({audio:{mandatory:{chromeMediaSource:'tab'}}})`;
- `chrome.offscreen.createDocument()` — offscreen-документ, который в Chrome нужен только потому,
  что MV3 service worker не имеет доступа к `AudioContext`.

В Firefox фоновый скрипт MV3 — это event page, а не service worker, но передать в него звук вкладки
всё равно нечем. Поэтому граф Web Audio перенесён **внутрь страницы**: content script
`eq-content.js` подключает элементы `<audio>` и `<video>` через `createMediaElementSource()` и
пропускает их через общую цепочку фильтров. Offscreen-документ больше не нужен.

### ⚠️ Важное отличие в поведении

Chrome-версия обрабатывала **весь** звук вкладки. Эта версия обрабатывает только медиаэлементы
страницы. Звук, который сайт синтезирует сам через Web Audio API (некоторые браузерные игры,
звонки), перехватить нельзя — расширение честно сообщит об этом вместо тихого отказа.

Так же обрабатываются случаи, когда медиа отдаётся с другого домена без CORS-заголовков: граф
Web Audio в этом случае выдаёт тишину, поэтому Ears определяет это и возвращает исходное
воспроизведение.

## ✨ Возможности

- Параметрический эквалайзер с интерактивными точками фильтров.
- Быстрый пресет **Bass Boost**.
- Сохранение, удаление, импорт и экспорт пользовательских пресетов.
- Спектральный визуализатор.
- Поддержка нескольких активных вкладок одновременно.
- Кнопка запуска в отдельном окне (Full Window).

## 📦 Установка (локально, для разработки)

1. Скачайте/клонируйте репозиторий.
2. Откройте `about:debugging#/runtime/this-firefox`.
3. Нажмите **Load Temporary Add-on…** / **Загрузить временное дополнение…**.
4. Выберите файл `manifest.json` из папки проекта.

Временное дополнение живёт до перезапуска браузера. Для постоянной установки требуется подпись
через [AMO](https://addons.mozilla.org/developers/).

## 🧭 Как пользоваться

1. Откройте вкладку с аудио/видео.
2. Нажмите на иконку `Ears` — текущая вкладка подключается автоматически. Кнопка внизу
   переключает состояние (**Stop EQing This Tab** / **EQ This Tab**).
3. Настройте фильтры на вкладке **Controls**.
4. При необходимости сохраните пресет (кнопка **+ Save Preset**).
5. Для переноса настроек используйте **Export Presets** / **Import Presets**.

Чтобы добавить ещё одну вкладку, перейдите на неё и повторите шаг 2.

## 🔐 Разрешения

- `activeTab` — доступ к текущей вкладке, выдаётся по клику на иконку расширения. Это основной путь:
  отдельного запроса прав не требуется.
- `host_permissions: <all_urls>` — в Firefox MV3 это **опциональное** разрешение, по умолчанию не
  выданное. Разрешите Ears работать на всех сайтах (`about:addons` → Ears → Permissions), если
  хотите, чтобы EQ переживал переходы по страницам без повторного клика по иконке.
- `scripting` — внедрение `eq-content.js` в обрабатываемую вкладку.
- `storage` — пресеты (`storage.local`) и текущее состояние EQ (`storage.session`).
- `tabs` — список активных вкладок и отслеживание их закрытия/навигации.
- `downloads` — экспорт пресетов в файл.

## 🏗️ Структура проекта

- `manifest.json` — конфигурация расширения (MV3 для Gecko, event page).
- `bg.js` — фоновый event page: состояние EQ, пресеты, маршрутизация сообщений.
- `eq-content.js` — content script с графом Web Audio (фильтры, gain, анализатор).
- `popup.html`, `popup.css`, `popup.js` — UI и логика всплывающего окна.
- `snap.svg-min.js` — библиотека для работы с SVG в UI.
- `ears*.png`, `earstile*.*` — иконки и графические ассеты.

## 🧪 Разработка и проверка

Проект не требует сборки: это unpacked extension со статическими файлами.

Быстрые проверки после изменений:

```bash
python -m json.tool manifest.json >/dev/null
node --check bg.js
node --check eq-content.js
node --check popup.js
```

Логи фонового скрипта: `about:debugging#/runtime/this-firefox` → **Inspect** рядом с Ears.

## ⚠️ Disclaimer

Сильное усиление частот может повредить слух и/или акустику. Используйте эквалайзер ответственно.
